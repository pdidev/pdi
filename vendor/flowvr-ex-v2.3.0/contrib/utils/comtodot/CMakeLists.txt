PROJECT(comtodot)
   
SET(BIN_DIR bin)

FIND_PACKAGE(FlowVR REQUIRED)

INCLUDE_DIRECTORIES(${FLOWVR_INCLUDE_DIR})

ADD_EXECUTABLE(comtodot comtodot.cpp)
TARGET_LINK_LIBRARIES(comtodot ${FLOWVR_base_LIBRARY} ${FLOWVR_app_LIBRARY} dl)
INSTALL(TARGETS comtodot RUNTIME DESTINATION ${BIN_DIR})
