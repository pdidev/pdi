INCLUDE_DIRECTORIES(../include/  ${FLOWVR_INCLUDE_DIR})

ADD_EXECUTABLE(joypad joypad.cpp)
TARGET_LINK_LIBRARIES(joypad ${FLOWVR_base_LIBRARY} ${FLOWVR_mod_LIBRARY} ${CMAKE_THREAD_LIBS_INIT})

INSTALL(TARGETS joypad DESTINATION ${MODULE_BIN_DIR})

