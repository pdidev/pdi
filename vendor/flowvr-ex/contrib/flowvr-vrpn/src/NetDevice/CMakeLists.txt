IF(BUILD_FLOWVR_VRPN_SIMULATED_INTERFACE)

  INCLUDE_DIRECTORIES(../../include/flowvr-vrpn/core/ ${FLOWVR_INCLUDE_DIR} ${VRPN_INCLUDES} ${QUAT_INCLUDES} ${GLUI_INCLUDES})
  
  SET(NetDevice
    NetDevice.cpp)

  ADD_EXECUTABLE(netdevice ${NetDevice})
  

  
  TARGET_LINK_LIBRARIES(netdevice ${FLOWVR_base_LIBRARY} ${FLOWVR_ftl_LIBRARY} ${FLOWVR_mod_LIBRARY} ${OPENGL_LIBRARIES} ${GLUT_LIBRARIES} ${VRPN_LIBRARY} ${QUAT_LIBRARY} ${GLUI_LIBRARY})
  
  INSTALL(TARGETS netdevice  DESTINATION ${MODULE_DIR})
ENDIF(BUILD_FLOWVR_VRPN_SIMULATED_INTERFACE)
