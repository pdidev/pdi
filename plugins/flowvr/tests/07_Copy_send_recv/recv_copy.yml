logging: trace

data:
  wait: int
  message: {type: array, subtype: int, size: 32}

plugins:
  flowvr:
    abort_on: "abort"
    wait_on_data: wait
    input_ports:
      message:
        data: message