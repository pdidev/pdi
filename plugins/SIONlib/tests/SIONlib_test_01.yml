pdi:
  metadata:
    meta0: int
    meta1: int
    meta2: int
    meta3: int
    meta4: int
  data: # values that can not be referenced using $syntax
    test_var: double
  plugins:
    SIONlib:
      outputs:
        test_var:
          directory: .
          generation: $meta1
          select: 1
