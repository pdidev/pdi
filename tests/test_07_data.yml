beta: 0.250 # Can't be loaded with `PC_double(PC_get(conf, ".alpha"), &alpha);` as this is not the main YAML

data:
  array_data:
    type: array
    subtype: int
    size: 8
    subsize: 4
    start: 2
  record_data:
    type: record
    buffersize: 16
    members:
      a:
        disp: 0
        type: int
      b:
        disp: 8
        type: pointer
        subtype: int
