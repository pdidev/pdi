beta: 0.250 # Can't be loaded with `PC_double(PC_get(conf, ".alpha"), &alpha);` as this is not the main YAML

include:
  - test_07_subdata.yml
  - test_07.yml
data:
  scalar_subdata: int
  array_data:
    type: array
    subtype: int
    size: 8
    subsize: 4
    start: 2
