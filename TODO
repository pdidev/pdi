* rename `params` to `metadata` & `variables` to `data` in config
* correctly handle dimensions in hdf5
* error handling in hdf5
* debug plugin
* support structs
* handle nested arrays in HDF5
* support constructing and modifying types from code
* Rename the error handling functions & macros in `status.h` to something more sensible
  - `handle_error` to `make_error`
  - `handle_err` to `throw_error`
*